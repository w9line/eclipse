<div align="center">

# ECLIPSE

### EExamine Commit Logs Identify Potential Secret Exposures 

[![Python](https://img.shields.io/badge/Python-3.11+-000?style=for-the-badge&logo=python&logoColor=white)](https://www.python.org/)
[![FastAPI](https://img.shields.io/badge/FastAPI-0.100+-000?style=for-the-badge&logo=fastapi&logoColor=white)](https://fastapi.tiangolo.com/)
[![Vue.js](https://img.shields.io/badge/Vue.js-3.4+-000?style=for-the-badge&logo=vuedotjs&logoColor=white)](https://vuejs.org/)

**Автоматическое обнаружение утечек секретов в Git-репозиториях**

[Возможности](#-возможности) •
[Установка](#-установка) •
[Использование](#-использование) •
[API](#-api) •
[Документация](#-документация)

---

</div>

## О проекте

**ECLIPSE** — инструмент безопасности класса Blue Team для автоматического обнаружения утечек конфиденциальных данных в Git-репозиториях.

Как солнечное затмение обнажает корону звезды, ECLIPSE выявляет скрытые секреты в вашем коде — включая те, что были "удалены", но остались в истории коммитов.

### Проблема

> По данным GitGuardian, в 2023 году обнаружено **12.8 млн** утечек секретов в публичных репозиториях. Среднее время обнаружения — **327 дней**.

Даже после удаления, секреты остаются в Git-истории и могут быть извлечены злоумышленником.

### Решение

ECLIPSE сканирует **всю историю** репозитория и находит:

- API-ключи и токены
- Приватные ключи
- Пароли и credentials
- Строки подключения к БД
- Персональные данные
- Метаданные документов

---

## Возможности

### Обнаружение секретов

- 25+ паттернов (AWS, GCP, GitHub, Stripe...)
- Анализ энтропии Шеннона
- Приватные ключи (RSA, SSH, PGP)
- JWT токены

### Анализ истории

- Сканирование всех коммитов
- Все ветки репозитория
- "Удалённые" файлы
- Временная шкала находок

### Метаданные

- EXIF из изображений (GPS!)
- Авторы документов Office
- Метаданные PDF
- Внутренние хосты в конфигах

### Отчётность

- Экспорт: JSON, CSV, MD, HTML
- Классификация по критичности
- Рекомендации по устранению
- Веб-интерфейс

---

## Архитектура

```
eclipse/
├── backend/                 # FastAPI сервер
│   ├── main.py             # REST API
│   ├── models.py           # SQLAlchemy модели
│   ├── schemas.py          # Pydantic схемы
│   ├── scanner_wrapper.py  # Интеграция сканера
│   └── git_utils.py        # Работа с Git
│
├── core/                    # Ядро сканера
│   ├── core_scanner.py     # Паттерны + энтропия
│   └── metadata_analyzer.py # Извлечение метаданных
│
├── frontend/                # Vue.js интерфейс
│   ├── src/
│   │   ├── App.vue         # Главный компонент
│   │   └── components/     # UI компоненты
│   └── index.html
│
└── repos/                   # Клонированные репозитории
```

---

## Установка

### Требования

- Python 3.11+
- Node.js 18+
- Git

### Быстрый старт

```bash
# Клонирование
git clone https://github.com/username/eclipse.git
cd eclipse

# Backend
cd backend
pip install -r requirements.txt

# Frontend
cd ../frontend
npm install
```

### Зависимости Backend

```txt
fastapi>=0.100.0
uvicorn>=0.23.0
sqlalchemy>=2.0.0
python-multipart>=0.0.6
pillow>=10.0.0
pypdf>=3.0.0
python-docx>=0.8.11
openpyxl>=3.1.0
python-pptx>=0.6.21
```

---

## Использование

### Запуск

```bash
# Терминал 1 — Backend
cd backend
uvicorn backend.main:app --reload --port 8000

# Терминал 2 — Frontend
cd frontend
npm run dev
```

Откройте **http://localhost:5173**

### Интерфейс

1. **Добавить репозиторий** — по URL или локальному пути
2. **Сканировать** — анализ файлов и истории
3. **Фильтровать** — по категории и критичности
4. **Экспортировать** — отчёт в нужном формате

---

## Классификация

### Категории

| Категория | Описание | Примеры |
|-----------|----------|---------|
| `secret` | Секреты и ключи | AWS, GitHub, Stripe tokens |
| `infra` | Инфраструктура | Database URIs, внутренние IP |
| `pii` | Персональные данные | Email, телефоны |
| `metadata` | Метаданные файлов | Авторы, GPS-координаты |

### Уровни критичности

| Уровень | Описание | Примеры |
|---------|----------|---------|
| **Critical** | Критические секреты | Приватные ключи, Stripe Live |
| **High** | Высокий риск | GitHub/Telegram токены, DB URI |
| **Medium** | Средний риск | JWT secrets, generic passwords |
| **Low** | Низкий риск | Emails, метаданные |

---

## 🔌 API

### Репозитории

```bash
# Добавить репозиторий
POST /api/repos
{"path": "https://github.com/user/repo", "is_url": true}

# Список репозиториев
GET /api/repos

# Удалить репозиторий
DELETE /api/repos/{id}
```

### Сканирование

```bash
# Запустить скан
POST /api/scans?repo_id=1

# Статус скана
GET /api/scans/{id}

# Результаты
GET /api/scans/{id}/findings
GET /api/scans/{id}/findings?severity=critical
GET /api/scans/{id}/findings?category=secret
```

### Экспорт

```bash
# JSON
GET /api/scans/{id}/export/json

# CSV
GET /api/scans/{id}/export/csv

# Markdown
GET /api/scans/{id}/export/markdown

# HTML (красивый отчёт)
GET /api/scans/{id}/export/html
```

---

## Тестирование

Генератор тестового репозитория с уязвимостями:

```bash
cd test_system
python generate_test_repo.py

# Создаст eclipse_test_repo/ с:
# - 25+ типов секретов
# - Секреты в истории (удалённые)
# - Метаданные
# - Несколько веток
```

---

## Обнаруживаемые секреты

### Облачные провайдеры

- AWS Access Key ID / Secret Access Key
- GCP API Key / Service Account
- Azure Storage Key
- Firebase API Key

### Платформы разработки

- GitHub Personal Access Token
- GitHub Fine-grained Token
- GitLab Personal Token
- Bitbucket App Password

### Мессенджеры и боты

- Telegram Bot Token
- Discord Bot Token
- Slack Token (xoxb, xoxp, xoxa)

### Платёжные системы

- Stripe Secret Key / Restricted Key
- PayPal Bearer Token

### Базы данных

- PostgreSQL URI
- MySQL URI
- MongoDB URI
- Redis URI

### Криптография

- RSA Private Key
- DSA Private Key
- EC Private Key
- OpenSSH Private Key
- PGP Private Key

### Общие

- JWT Tokens
- Generic Passwords
- Generic Secrets
- High-entropy Strings

---

## Конфигурация

### Кастомные правила

Создайте `rules.json` в корне сканируемого репозитория:

```json
{
  "rules": [
    {
      "name": "internal_api_key",
      "pattern": "INTERNAL_KEY_[A-Z0-9]{32}"
    },
    {
      "name": "custom_token",
      "pattern": "myapp_[a-z0-9]{40}"
    }
  ]
}
```

### Параметры сканера

```python
ScanConfig(
    repo_path=Path("./repo"),
    scan_history=True,           # Сканировать историю
    history_commit_limit=100,    # Лимит коммитов
    max_file_size=1_000_000,     # Макс. размер файла (1MB)
    entropy_threshold=4.5,       # Порог энтропии
    include_patterns=True,       # Использовать паттерны
    include_entropy=True,        # Использовать энтропию
)
```

---

## Blue Team

ECLIPSE — инструмент направления **Blue Team**:

- **Проактивная защита** — находит проблемы до атаки
- **Предотвращение** — не эксплуатирует уязвимости
- **Мониторинг** — регулярный аудит репозиториев
- **Отчётность** — документация для compliance

---

## Roadmap

- [ ] Интеграция с CI/CD (GitHub Actions, GitLab CI)
- [ ] Валидация токенов (проверка активности)
- [ ] Pre-commit hook
- [ ] Уведомления (Slack, Telegram, Email)
- [ ] ML-классификатор для снижения false positives
- [ ] Сканирование Docker images

---

## Благодарности

- [FastAPI](https://fastapi.tiangolo.com/) — асинхронный веб-фреймворк
- [Vue.js](https://vuejs.org/) — прогрессивный JS-фреймворк
- [Tailwind CSS](https://tailwindcss.com/) — utility-first CSS
- [Gitleaks](https://github.com/gitleaks/gitleaks) — вдохновение для паттернов

---

<div align="center">

**ECLIPSE**

*Затмевает угрозы до того, как они затмят вас*

</div>
